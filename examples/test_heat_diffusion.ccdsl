# Simple heat diffusion test for MVP verification
# This is a minimal example to test the runtime

# Create a small temperature field
temp = field.alloc((64, 64), fill_value=0.0)

# Add heat in the center
# Note: For MVP we'll need to implement this via field operations
# For now, this tests field allocation

# Diffuse the heat
temp = field.diffuse(temp, rate=0.1, dt=0.01)

# Apply boundary conditions
temp = field.boundary(temp, spec="reflect")

# Visualize
vis = visual.colorize(temp, palette="fire")
visual.output(vis, path="test_heat.png")
